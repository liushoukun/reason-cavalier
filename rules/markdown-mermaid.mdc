---
description: Markdown 文档中图示统一使用 Mermaid
globs: "**/*.md"
alwaysApply: false
---

# Markdown Mermaid Rule

当需要在 Markdown 中表达流程图、时序图、结构图时，必须使用 Mermaid 语法。

示例：

```mermaid
flowchart TD
  A[需求] --> B[拆解任务]
  B --> C[执行与验证]
```
